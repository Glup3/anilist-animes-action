query MyAnimes($username: String) {
  MediaListCollection(userName: $username, type: ANIME) {
    lists {
      name
      entries {
        media {
          title {
            romaji
          }
          coverImage {
            large
          }
        }
      }
    }
  }
}

# https://graphql.anilist.co
